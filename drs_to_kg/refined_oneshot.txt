You are an expert in Pancreatic Cancer, Discourse Representation Structures (DRS), and Knowledge Graph engineering.
Convert the given DRS input into valid Turtle/RDF that strictly follows the ontology and structural rules below.

### 1. Allowed Classes
Use only these classes when assigning `rdf:type`:
- DRS logic: `lkg:Formula`, `lkg:DRSBox`, `lkg:LogicalOperator`, `lkg:Negation`, `lkg:Conjunction`, `lkg:Disjunction`, `lkg:Implication`
- Medical entities: `lkg:Species`, `lkg:Disease`, `lkg:Chemical`, `lkg:Gene`, `lkg:Variant`, `lkg:CellLine`

### 2. Allowed Properties
Use only these properties:
- DRS logic: `lkg:hasAntecedent`, `lkg:hasConsequent`, `lkg:hasOperand`
- DRS scope: `lkg:containsEntity`, `lkg:containsStatement`
- Statement structure: `rdf:subject`, `rdf:predicate`, `rdf:object`
- Medical relations: use only ontology-defined relations such as `lkg:treat`, `lkg:associate`, etc.

### 3. Critical Rules
1. Return ONLY raw Turtle/RDF. No markdown, no explanation.
2. Always use `rdf:type`, never `a`.
3. Do not invent classes, properties, entities, or relations.
4. Normalize names with underscores and English labels only.
5. Every unique RDF triple must appear exactly once.
6. Do not regenerate previously emitted entities, DRS boxes, or statements.
7. After all input lines are covered exactly once, stop immediately.

### 4. One-Fact-One-Structure
Apply these rules exactly:
- For each `exist` predicate, create exactly one `lkg:DRSBox` with exactly one `lkg:containsEntity`.
- For each binary relation predicate, create exactly one `lkg:DRSBox` and exactly one `rdf:Statement`.
- Do not merge unrelated input facts into one DRSBox.
- Do not create multiple DRSBoxes or multiple rdf:Statements for the same input line.

### 5. Deduplication Checklist
Before finishing, ensure:
- no duplicated entity type triple,
- no duplicated DRSBox block,
- no duplicated rdf:Statement block,
- no duplicated subject-predicate-object triple.

### 6. Conversion Example
Input:
"[A] predicate(A,exist,named(Chemical_example_01))-1/4
[A] predicate(A,exist,named(Disease_example_01))-1/4
[A] predicate(A,treat,named(Chemical_example_01),named(Disease_example_01))-1/6"

Output:
lkg:Chemical_example_01 rdf:type lkg:Chemical .
lkg:Disease_example_01 rdf:type lkg:Disease .

lkg:DRS_01 rdf:type lkg:DRSBox ;
    lkg:containsEntity lkg:Chemical_example_01 .

lkg:DRS_02 rdf:type lkg:DRSBox ;
    lkg:containsEntity lkg:Disease_example_01 .

lkg:DRS_03 rdf:type lkg:DRSBox ;
    lkg:containsEntity lkg:Chemical_example_01 , lkg:Disease_example_01 ;
    lkg:containsStatement lkg:STMT_03 .

lkg:STMT_03 rdf:type rdf:Statement ;
    rdf:subject lkg:Chemical_example_01 ;
    rdf:predicate lkg:treat ;
    rdf:object lkg:Disease_example_01 .

Now, convert the following input text into valid Knowledge Graph:
