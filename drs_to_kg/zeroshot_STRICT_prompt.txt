You are an expert in Pancreatic Cancer, Discourse Representation Structures (DRS), and Knowledge Graph engineering.
Your task is to analyze the provided list of DRS formatted text from a research paper and extract knowledge information into a structured RDF format that strictly adheres to the provided Ontology rules.

### 1. Ontology Rules (Reference Schema)
The extracted data must strictly follow these Entity Types and Relation rules:

[Entity Types]

Logical Operator Layer (DRS Logic)
lkg:Formula : The top-level abstract class for any logical structure or expression.
lkg:DRSBox : A logical container/box that holds factual statements and entities.
lkg:LogicalOperator : A logical connector used to handle complex conditions or operators.
lkg:Negation : Represents logical NOT (used for negative statements or absences).
lkg:Conjunction : Represents logical AND (used to join multiple true conditions).
lkg:Disjunction : Represents logical OR (used for alternative possibilities).
lkg:Implication : Represents logical IF-THEN (used for conditional or causal rules).

Medical Fact Layer (Statement Encapsulation)
lkg:MedEntity : The top-level class for all recognized biomedical concepts.
lkg:Species : Organisms, animal models, or human subjects used in the study.
lkg:Disease : Pathologies, clinical conditions, diagnoses, or syndromes.
lkg:Chemical : Drugs, small molecules, pharmacological compounds, or agents.
lkg:Gene : Specific genes, DNA sequences, RNA transcripts, or molecular targets.
lkg:Variant : Genetic mutations, polymorphisms, alleles, or structural variants.
lkg:CellLine : Cultured cells or in vitro cellular models used in experiments.

[Relation Rules (Domain -> Relation -> Range)]

Logical Operator Layer (DRS Logic)
- lkg:Implication -> lkg:hasAntecedent -> lkg:Formula (Connects the antecedent/IF-clause)
- lkg:Implication -> lkg:hasConsequent -> lkg:Formula  (Connects the consequent/THEN-clause)
- lkg:Negation -> lkg:hasOperand -> lkg:Formula  (Connects the negated target)
- lkg:Conjunction -> lkg:hasOperand -> lkg:Formula (Connects DRSs to be joined by AND)
- lkg:Disjunction -> lkg:hasOperand -> lkg:Formula (Connects DRSs to be joined by OR)

DRS Box Inclusion Layer (Scope Isolation)
- lkg:DRSBox -> lkg:containsEntity -> lkg:MedEntity (Connects medical instances declared/mentioned inside this DRS)
- lkg:DRSBox -> lkg:containsStatement -> rdf:Statement (Connects the encapsulated condition triples belonging to this DRS)

Medical Fact Layer (Statement Encapsulation)
- rdf:Statement -> rdf:subject -> lkg:MedEntity (The subject entity of the relation)
- rdf:Statement -> rdf:predicate -> lkg:MedRelation (The specific medical relation property)
- rdf:Statement -> rdf:object -> lkg:MedEntity (The object entity of the relation)

### 2. Strict Syntax & Structural Rules (CRITICAL)
- DO NOT use abbreviations like 'a'. Always use 'rdf:type'.
- DO NOT invent or use properties not listed in the rules (e.g., DO NOT use rdfs:label).
- Normalize all entity names by converting spaces to underscores and using English.

### 3. Output Constraints
1. FORMAT: Return ONLY the raw Turtle/RDF text. Do not include markdown code blocks (```turtle), introductions, or conclusions. Start directly with the data.
2. INDIVIDUAL TYPE DECLARATION: Explicitly declare the `rdf:type` for EVERY single extracted instance at the end of the file.
3. FAITHFULNESS / NO HALLUCINATION: Do not invent, assume, or extrapolate any relationships or properties that are not explicitly stated in the input text. Only use the properties defined in the given instructions. If a relationship is not directly mentioned, do not create it.

Now, convert the following input text into valid Knowledge Graph:
