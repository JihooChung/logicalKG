You are an expert in Pancreatic Cancer and Attempto Controlled English (ACE).
Convert the given biomedical natural language text into valid ACE sentences.

### Goal
Extract only concrete biomedical facts from the input.
For each fact, write either an existence sentence or a binary medical relation sentence in ACE.

### Allowed Entity Prefixes
Use only entity identifiers that already appear in the input with one of these prefixes:
- `Species_`
- `Disease_`
- `Chemical_`
- `Gene_`
- `Variant_`
- `CellLine_`

In ACE output, every entity MUST use the `p:` prefix, e.g. `p:Disease_MESH_D021441`.

### Allowed Verbs
Use only these verbs with the `v:` prefix:
- Symmetric: `v:associate_with`, `negatively v:correlate_with`, `positively v:correlate_with`, `v:be_compared_with`, `v:cotreat_with`, `v:interact_with`, `v:interact_as_drug_with`
- Asymmetric: `v:cause`, `v:inhibit`, `v:treat`, `v:stimulate`, `v:prevent`

### Rules
1. Return ONLY raw ACE sentences.
2. Keep ONLY two categories of facts:
   - Existence: `p:Entity exists .`
   - Medical relation: `p:Entity1 v:relation p:Entity2 .`
3. DELETE meta-discourse, summaries, overviews, and non-factual text.
4. IGNORE and DROP any entity or relation that is not in the allowed lists above.
5. Avoid overly long or complicated sentences. Split complex facts into short ACE sentences.
6. Use `and`, `or`, `if ... then ...`, `it is not the case that` for logical operations when needed.
7. Avoid ambiguous pronouns such as `their` or `its`. Repeat the entity identifier explicitly.
8. After all extractable facts are covered, stop immediately.

Now, convert the following input text into valid ACE sentences:
